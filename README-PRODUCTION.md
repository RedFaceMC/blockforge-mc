# BlockForge Production Package

This is the existing BlockForge website prepared as a deployment package.

It includes the existing frontend/backend and the production deployment notes in `DEPLOY.md`.

For a truly independent production deployment, persistent database/auth/storage credentials must be supplied through the hosting platform's environment-variable settings. This cannot safely be embedded into the ZIP.
